# 🐛 현재 남아있는 버그

## 문제
노원구(및 다른 구)를 클릭하면 모든 동이 같은 색(짙은 초록색)으로 표시됨

## 원인 추정
1. 정규식 `/[0-9·]+가?$/g`가 실제 GeoJSON 데이터와 맞지 않을 수 있음
2. GeoJSON의 `adm_nm` 필드 형식을 정확히 확인 필요

## 다음 단계
1. **GeoJSON 동 이름 형식 확인**:
   ```javascript
   // 브라우저 콘솔에서 실행
   dongLayer.eachLayer(layer => {
     if (layer.feature.properties.sggnm === '노원구') {
       console.log(layer.feature.properties.adm_nm);
     }
   });
   ```

2. **정규식 테스트**: 
   - 예: "서울특별시 노원구 월계1동" → "월계동"
   - 현재 정규식이 `.split(' ').pop()` 이후에 적용됨
   - `"월계1동".replace(/[0-9·]+가?$/g, '')` → "월계동" ✓

3. **데이터베이스 동 이름 재확인**:
   ```bash
   python check_db.py
   ```

## 임시 디버깅 코드 추가 제안
`drillDownToGu` 함수에 다음 추가:
```javascript
console.log('Raw dong name:', rawDongName);
console.log('Normalized:', normalizedDongName);
console.log('DB has:', Object.keys(dongStats));
```

집에서 계속하실 때 이 내용을 참고하세요! 🏠
