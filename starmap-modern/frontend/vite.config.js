import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// ============================================================================
// 🎓 LEARNING: Vite 설정 파일
// ============================================================================
//
// 📌 Vite란?
// ----------------------------------------------------------------------------
// - 프론트엔드 빌드 도구 (번들러)
// - Create React App (CRA)보다 훨씬 빠름
// - 요즘 React 프로젝트의 표준 빌드 도구
//
// 📌 번들러가 필요한 이유
// ----------------------------------------------------------------------------
// 1. 여러 파일을 하나로 합침 (번들링)
// 2. 최신 JS 문법을 구형 브라우저용으로 변환 (트랜스파일링)
// 3. JSX를 일반 JavaScript로 변환
// 4. CSS, 이미지 등 리소스 최적화
//
// 📌 개발 서버 vs 빌드
// - npm run dev: 개발 서버 실행 (실시간 반영, 디버깅 가능)
// - npm run build: 배포용 파일 생성 (최적화, 압축)
// ============================================================================

export default defineConfig({
    plugins: [react()],

    // 개발 서버 설정
    server: {
        port: 5173,  // 기본 포트
        open: true,  // 서버 시작 시 브라우저 자동 열기
    }
})
